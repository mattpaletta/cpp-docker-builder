FROM mattpaletta:cpp-build

RUN apt-get update && apt-get install --no-install-suggests --no-install-recommends -y \
  	vim && \
	&& apt-get clean

RUN git clone https://github.com/mattpaletta/scripts.git && \
	cd scripts && \
	bash install.sh vim && \
	cd / && \
	rm -rf scripts
